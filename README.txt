Django Blog:

A web application built using Python, Javascript, and Bootstrap using the Django web framework. The site
allows for users to post blog style info on topics they wish to discuss once they are registered. Users 
have profile pages and can upload images to them witch are tracked and resized for space on the server 
with the python lib pillows all data entries are saved and clicking on a user’s name will show all of 
their posts.

steps to run :

1. Build a virtual enviroment in python 3.6 or higher and move in code

2. Install libs using the requiremnets.txt file 

3. Run the manage.py file with the command runserver

Lead Developer : Matthew Cintron 

